#include "bzip2compressor.h"
#include <assert.h>

using namespace fsp::protocol::compressors;


_STD string Bzip2Compressor::compress(const _STD string_view Str)  const
{
	assert(0);

	return "";
}
_STD string Bzip2Compressor::decompress(const _STD string_view Str)  const
{
	assert(0);

	return "";
}